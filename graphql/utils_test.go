package graphql

/*func TestProcessRef(t *testing.T) {
	type test struct {
		description     string
		inputPath       string
		inputDefKeyword string
		wantError       bool
	}

	tests := []test{
		{
			description:     "should parse a valid external file ref path",
			inputPath:       "./simple-schema.json",
			inputDefKeyword: "$defs",
			wantError:       false,
		},
	}

	for _, test := range tests {
		t.Run(test.description, func(t *testing.T) {
			err := proces
		})
	}
}*/
